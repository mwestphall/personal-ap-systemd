---
batch_connect:
  template: basic
script:
  native:
    - "-p"
    - "<%= slurm_partition %>"
    - "-c"
    - "<%= cpus %>"
    - "--mem"
    - "<%= memory %>G"
    - "-t"
    - "<%= time_limit %>"
