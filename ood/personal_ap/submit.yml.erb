---
batch_connect:
  template: basic
script:
  native:
    - "-p"
    - "<%= context.slurm_partition %>"
    - "-c"
    - "<%= context.cpus %>"
    - "--mem"
    - "<%= context.memory %>G"
    - "-t"
    - "<%= context.time_limit %>"
